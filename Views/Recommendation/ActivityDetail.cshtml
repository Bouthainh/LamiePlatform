@model LamiePlatform.Models.ViewModels.RecommendationDetailViewModel

@{
    ViewData["Title"] = "تفاصيل التوصية";
}

<div class="rec-outer" dir="rtl">
    <img src="~/images/starLogo.png" class="rec-star-logo" alt="" />

    <h2 class="rec-heading-secondary">تفاصيل التوصية</h2>

    <div class="rec-inner">
        <div class="rec-table-detail">
            <table>
                <tbody>
                    <tr>
                        <th>اسم النشاط</th>
                        <td>@Model.ActivityName</td>
                    </tr>
                    <tr>
                        <th>الفئة</th>
                        <td>@Model.Category</td>
                    </tr>
                    <tr>
                        <th>الوصف</th>
                        <td>@Model.ActivityDescription</td>
                    </tr>
                    <tr>
                        <th>المدة (دقيقة)</th>
                        <td>@(Model.Duration.HasValue ? Model.Duration.Value.ToString() : "غير محدد")</td>
                    </tr>
                    <tr>
                        <th>الطفل</th>
                        <td>@Model.ChildName</td>
                    </tr>
                </tbody>
            </table>
        </div>

        <div class="mt-3 text-end">
            <a asp-action="ActivityList" asp-route-childId="@Model.ChildId" class="btn-back">
                العودة إلى القائمة
            </a>
        </div>
    </div>
</div>
